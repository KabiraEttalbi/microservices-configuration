server:
  port: 8082  # Port différent pour le service product

spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/product
    password: root2024
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
